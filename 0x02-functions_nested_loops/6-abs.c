#include "main.h"
#include <stdio.h>

/**
 * _abs - print the absolute value of the integer
 * @n: the integer used
 * Return: the absolute value of the integer
 */
int _abs(int n)
{
	return (abs(n));
}
